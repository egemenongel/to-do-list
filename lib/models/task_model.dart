class TaskModel {
  String title;
  bool isCompleted = false;

  TaskModel({
    required this.title,
    required this.isCompleted,
  }) {
    this.title = title;
    this.isCompleted = isCompleted;
  }
}
